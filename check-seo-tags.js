javascript:(function(){ const metas = document.getElementsByTagName('meta'); let description = "empty"; let keywords = "empty"; let s = ""; for (let i = 0; i < metas.length; i++) { s = metas[i].name || ""; switch (s.toLowerCase()) { case "description": description = metas[i].content; break; case "keywords": keywords = metas[i].content; break; } } const message = "Title: " + document.title + "\n\nDescription: " + description + "\n\nKeywords: " + keywords; window.prompt("ctrl + c or cmd + c to copy\n\n" + message, message); })();
